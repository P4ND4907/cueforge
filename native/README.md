# Native Layer

Root native work lives here so desktop/helper code does not get buried inside the web app.

Windows is the primary target for CueForge because the product depends on WASAPI-style evidence, Equalizer APO, default endpoints, communication endpoints, virtual mixers, and Windows gaming setups.
