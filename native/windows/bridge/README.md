# Windows Bridge

Native helper lane for read-only Windows setup evidence.

Scope:

- Default playback/recording endpoints.
- App sessions when available.
- Installed companion tools.
- Safe helper manifest output.

Boundary: no default-device changes, no driver installs, no hidden APO writes.
