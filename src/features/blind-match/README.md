# Blind Match

This-or-that preference learning.

Owns:

- A/B rounds.
- Hidden preference weights.
- Conservative EQ/dynamics/spatial preference adapters.

Output must feed `cueforgeStateV2.calibration.preferenceModel` and the personalization lab input contract.
