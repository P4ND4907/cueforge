# Shared Layer

Shared code is for contracts that multiple features consume.

- `privacy` owns export/evidence privacy boundaries.
- `audio` owns analyzer and metrics adapters.
- `state` owns CueForge State, Brain, and state anchors.
- `schemas` documents versioned contracts.

Avoid putting page-specific UI or product copy here.
