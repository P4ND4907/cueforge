export function collectDefaultEndpoints() {
  return {
    schema: 'cueforge.native-default-endpoints.v1',
    supported: false,
    message: 'Default endpoint collection is implemented by the explicit Windows bridge, not by browser code.'
  };
}
