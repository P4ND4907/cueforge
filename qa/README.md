# QA Layer

QA artifacts live outside `src` so source code, generated proof, and fixture evidence do not blur together.

Use this layer for Playwright, Electron smoke, deterministic audio fixtures, hardware-profile scenarios, analyzer baselines, and release reports.
