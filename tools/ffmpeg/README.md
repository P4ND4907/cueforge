# FFmpeg Tooling

Future local FFmpeg adapters and helper scripts belong here.

Use for `astats`, `silencedetect`, `ebur128`, `aphasemeter`, and `axcorrelate` reference checks. Do not commit raw captured audio by default.
